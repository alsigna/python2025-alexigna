# docker compose build
# docker compose up

services:
  redis:
    image: redis:latest
    ports:
      - "6379:6379"

  worker:
    build:
      context: .
      dockerfile: dockerfile
    image: worker:latest
    depends_on:
      - redis
    environment:
      REDIS_HOST: redis


  # rq-dashboard:
  #   image: worker:latest
  #   depends_on:
  #     - redis
  #   environment:
  #     REDIS_HOST: redis
  #   command: rq-dashboard --redis-url redis://redis:6379/0
  #   ports:
  #     - "9181:9181"
    
